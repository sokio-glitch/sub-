name: Build LobeChat for Termux (Final Fix)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4
        with:
          repository: lobehub/lobe-chat
          ref: main
          fetch-depth: 1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9
          run_install: false

      - name: Install Dependencies
        run: pnpm install

      # 3. 关键修复：根据报错设置 AUTH_SECRET
      - name: Build Standalone
        run: pnpm build
        env:
          NEXT_OUTPUT: standalone
          # 依然跳过验证，防止连接真实数据库
          SKIP_ENV_VALIDATION: true
          
          # --- 核心修复 START ---
          # 报错明确指出必须存在 AUTH_SECRET
          AUTH_SECRET: "dummy_secret_for_build_process_32_chars_long"
          # 为了保险起见，保留 KEY_VAULTS_SECRET
          KEY_VAULTS_SECRET: "dummy_secret_for_build_process_32_chars_long"
          # --- 核心修复 END ---

          # 提供数据库 URL 格式欺骗验证
          DATABASE_URL: "postgresql://postgres:password@localhost:5432/lobe"
          APP_URL: "http://localhost:3000"

      - name: Package Application
        run: |
          echo "📦 开始打包..."
          mkdir -p package/public
          mkdir -p package/.next/static

          # 复制核心文件
          # 注意：有时候 standalone 结构会包含 .next/standalone/lobe-chat，这里直接复制内容
          cp -r .next/standalone/* package/

          # 复制静态资源
          cp -r .next/static package/.next/
          
          # 复制 Public
          cp -r public package/

          # ⚠️ 删除 sharp (Termux 需要自行安装)
          echo "🚫 删除 x64 架构的 sharp..."
          rm -rf package/node_modules/sharp
          rm -rf package/node_modules/@img/sharp-libvips-linux-x64

          # 压缩
          echo "🗜️ 正在压缩..."
          cd package
          tar -czvf ../lobe-termux.tar.gz .
          echo "✅ 打包完成！"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: lobechat-build-fixed-v2
          path: lobe-termux.tar.gz
          retention-days: 3
          NEXT_OUTPUT: standalone
          # 依然保留跳过验证，防止校验数据库连接
          SKIP_ENV_VALIDATION: true
          
          # --- 修复部分 START ---
          # ❌ 必须删除: NEXT_AUTH_SECRET (会导致构建失败)
          # ✅ 新增: LobeChat v2 使用 LOBE_AUTH_SECRET 作为主密钥
          LOBE_AUTH_SECRET: "dummy_secret_for_build_process_32_chars_long"
          # --- 修复部分 END ---

          # 提供其他必要的假变量
          DATABASE_URL: "postgresql://postgres:password@localhost:5432/lobe"
          KEY_VAULTS_SECRET: "dummy_secret_for_build_process"
          APP_URL: "http://localhost:3000"

      # 4. 打包处理 (针对 Termux 的特殊处理)
      - name: Package Application
        run: |
          echo "📦 开始打包..."
          # 创建目录结构
          mkdir -p package/public
          mkdir -p package/.next/static

          # 复制 Standalone 核心文件
          # 注意：LobeChat 的 standalone 目录结构可能较深，通常在 .next/standalone/lobe-chat 或 .next/standalone
          # 这里使用通配符确保复制内容正确
          cp -r .next/standalone/* package/

          # 复制静态资源 (解决样式和脚本丢失)
          cp -r .next/static package/.next/

          # 复制 Public (解决图标和字体丢失)
          cp -r public package/

          # ⚠️ 关键：删除 sharp
          # GitHub Action 是 x64 环境，编译出的 sharp 无法在 Termux (ARM64) 运行
          # 必须删除，然后在手机上重新安装
          echo "🚫 删除 x64 架构的 sharp..."
          rm -rf package/node_modules/sharp
          rm -rf package/node_modules/@img/sharp-libvips-linux-x64

          # 压缩
          echo "🗜️ 正在压缩..."
          cd package
          tar -czvf ../lobe-termux.tar.gz .
          echo "✅ 打包完成！"

      # 5. 上传产物
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: lobechat-build-fixed
          path: lobe-termux.tar.gz
          retention-days: 3
        env:
          NEXT_OUTPUT: standalone
          # 绕过 T3 Env 或 Zod 的校验
          SKIP_ENV_VALIDATION: true 
          # 提供假的必须变量，防止构建崩溃
          DATABASE_URL: "postgresql://postgres:password@localhost:5432/lobe"
          KEY_VAULTS_SECRET: "dummy_secret_for_build_process"
          NEXT_AUTH_SECRET: "dummy_secret_for_build_process"
          APP_URL: "http://localhost:3000"

      # 4. 打包处理 (解决架构不兼容问题)
      - name: Package Application
        run: |
          echo "📦 开始打包..."
          mkdir -p package/public
          mkdir -p package/.next/static

          # 复制核心文件
          cp -r .next/standalone/* package/
          # 复制静态资源 (解决样式丢失)
          cp -r .next/static/* package/.next/static/
          # 复制 Public (解决图标丢失)
          cp -r public/* package/public/
          
          # ⚠️ 关键：删除 sharp (Linux x64)，防止在手机 (ARM64) 上报错
          # 我们会在 Termux 脚本里重新安装它
          rm -rf package/node_modules/sharp

          # 压缩
          cd package
          tar -czvf ../lobe-termux.tar.gz .
          echo "✅ 打包完成！"

      # 5. 上传产物
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: lobechat-build-fixed
          path: lobe-termux.tar.gz
          retention-days: 3
